# ==============================================================================
# Keybindings
# See laptops.conf for more
# See https://wiki.hyprland.org/Configuring/Keywords/ for more
# ==============================================================================

$scriptsDir = $HOME/.config/hypr/scripts

# -----------------------
# Shell
# -----------------------
bind = Super, C, exec, $scriptsDir/sys-shell-change.sh

# -----------------------
# Launcher
# -----------------------
bind = Super, D, exec, pkill rofi || rofi -show drun -modi drun
bind = Super, A, exec, pkill rofi || true && ags -t 'overview'

# -----------------------
# Applications / Quick actions
# -----------------------
bind = Super, T, exec, kitty
bind = Super, F, exec, thunar
bind = Super, Z, exec, pypr zoom
bind = Super, N, exec, swaync-client -t -sw
bind = Super, Y, exec, $scriptsDir/apps-keyhints.sh
bind = Super, E, exec, $scriptsDir/apps-quickedit.sh
bind = Super, S, exec, $scriptsDir/apps-rofi-beat.sh
bind = Super, V, exec, $scriptsDir/apps-clipboard.sh
bind = Super, R, exec, $scriptsDir/hypr-refresh.sh
bind = Super, Escape, exec,  $scriptsDir/apps-wlogout.sh        
bind = Super Ctrl, R, exec, $scriptsDir/hypr-refresh.sh --no-waybar
bind = Super Shift, E, exec, $scriptsDir/apps-rofi-emoji.sh       
bind = Super Shift, T, exec, pypr toggle term
bind = Super Shift, C, exec, hyprpicker -a 

# -----------------------
# Wallpaper / Waybar
# -----------------------
bind = Super, W,       exec, $scriptsDir/apps-wall-select.sh
bind = Ctrl ALT, W,    exec, $scriptsDir/apps-wall-random.sh  
bind = Super Ctrl, W,  exec, $scriptsDir/apps-waybar-layout.sh
bind = Super Shift, W, exec, $scriptsDir/apps-waybar-style.sh
bind = Super, B, exec, killall -SIGUSR1 waybar               

# -----------------------
# Hyprland
# -----------------------
bind = Super, P, pin
bind = Super, Q, killactive
bind = Super, M, fullscreen, 1
bind = Super, Space, exec, $scriptsDir/hypr-layout.sh  
bind = Super Shift, M, fullscreen, 0
bind = Super Shift, F, togglefloating
bind = Super Shift, R, exec, hyprctl reload
bind = Super Shift, B, exec, $scriptsDir/hypr-blur.sh    
bind = Super Shift, G, exec, $scriptsDir/hypr-gamemode.sh     
bind = Super Shift, Delete, exec, $scriptsDir/hypr-lock.sh
bind = Ctrl ALT, L, exec, $scriptsDir/hypr-lock.sh
bind = ALT, Space, exec, $scriptsDir/hypr-kb-switch.sh  

# -----------------------
# Screenshots
# -----------------------
bind = , Print,            exec, $scriptsDir/sys-screenshot.sh
bind = Super, Print,       exec, $scriptsDir/sys-screenshot.sh --now
bind = Super Shift, Print, exec, $scriptsDir/sys-screenshot.sh --area
bind = Super Ctrl, Print,  exec, $scriptsDir/sys-screenshot.sh --in5
bind = Super ALT, Print,   exec, $scriptsDir/sys-screenshot.sh --in10
bind = Super Shift, S,     exec, grim -g "$(slurp)" - | swappy -f -

# -----------------------
# Groups
# -----------------------
bind = Super, G,        togglegroup 
bind = Super Ctrl, tab, changegroupactive 

# -----------------------
# Navigation / Workspaces
# -----------------------
bind = Super, Tab,       workspace, m+1
bind = Super Shift, Tab, workspace, m-1
bind = ALT, Tab,         cyclenext

# Special workspace
bind = Super, U,       togglespecialworkspace
bind = Super Ctrl, U,  movetoworkspace, e+0
bind = Super Shift, U, movetoworkspace, special

# Switch workspaces with mainMod + [0-9]
bind = Super, 1, workspace, 1
bind = Super, 2, workspace, 2
bind = Super, 3, workspace, 3
bind = Super, 4, workspace, 4
bind = Super, 5, workspace, 5
bind = Super, 6, workspace, 6
bind = Super, 7, workspace, 7
bind = Super, 8, workspace, 8
bind = Super, 9, workspace, 9
bind = Super, 0, workspace, 10

# Move active window and follow to workspace
bind = Super Ctrl, 1, movetoworkspace, 1
bind = Super Ctrl, 2, movetoworkspace, 2
bind = Super Ctrl, 3, movetoworkspace, 3
bind = Super Ctrl, 4, movetoworkspace, 4
bind = Super Ctrl, 5, movetoworkspace, 5
bind = Super Ctrl, 6, movetoworkspace, 6
bind = Super Ctrl, 7, movetoworkspace, 7
bind = Super Ctrl, 8, movetoworkspace, 8
bind = Super Ctrl, 9, movetoworkspace, 9
bind = Super Ctrl, 0, movetoworkspace, 10
bind = Super Ctrl, bracketleft,  movetoworkspace, -1
bind = Super Ctrl, bracketright, movetoworkspace, +1

# Move active window to a workspace with mainMod + Shift + [0-9]
bind = Super Shift, 1, movetoworkspacesilent, 1
bind = Super Shift, 2, movetoworkspacesilent, 2
bind = Super Shift, 3, movetoworkspacesilent, 3
bind = Super Shift, 4, movetoworkspacesilent, 4
bind = Super Shift, 5, movetoworkspacesilent, 5
bind = Super Shift, 6, movetoworkspacesilent, 6
bind = Super Shift, 7, movetoworkspacesilent, 7
bind = Super Shift, 8, movetoworkspacesilent, 8
bind = Super Shift, 9, movetoworkspacesilent, 9
bind = Super Shift, 0, movetoworkspacesilent, 10
bind = Super Shift, bracketleft,  movetoworkspacesilent, -1
bind = Super Shift, bracketright, movetoworkspacesilent, +1

# Scroll through existing workspaces with mainMod + scroll / keys
bind = Super, mouse_down, workspace, e+1
bind = Super, mouse_up,   workspace, e-1
bind = Super, period,     workspace, e+1
bind = Super, comma,      workspace, e-1

# -----------------------
# Resize (vim-style)
# -----------------------
binde = Super Ctrl, H, resizeactive, -50 0
binde = Super Ctrl, L, resizeactive, 50 0
binde = Super Ctrl, K, resizeactive, 0 -50
binde = Super Ctrl, J, resizeactive, 0 50

binde = Super Ctrl, Left,  resizeactive, -50 0
binde = Super Ctrl, Right, resizeactive, 50 0
binde = Super Ctrl, Up,    resizeactive, 0 -50
binde = Super Ctrl, Down,  resizeactive, 0 50

# -----------------------
# Move (vim-style)
# -----------------------
bind = Super Shift, H, movewindow, l
bind = Super Shift, L, movewindow, r
bind = Super Shift, K, movewindow, u
bind = Super Shift, J, movewindow, d

bind = Super Shift, Left,  movewindow, l
bind = Super Shift, Right, movewindow, r
bind = Super Shift, Up,    movewindow, u
bind = Super Shift, Down,  movewindow, d

# -----------------------
# Move focus
# -----------------------
bind = Super, h, movefocus, l
bind = Super, l, movefocus, r
bind = Super, k, movefocus, u
bind = Super, j, movefocus, d

# -----------------------
# Mouse bindings (dragging)
# -----------------------
bindm = Super, mouse:272, movewindow
bindm = Super, mouse:273, resizewindow

# -----------------------
# Laptops
# -----------------------
bind = , xf86Sleep,          exec, systemctl suspend                             
bind = , xf86Launch1,        exec, rog-control-center                                       
bind = , xf86Launch4,        exec, asusctl profile -n                                      
bind = , xf86TouchpadToggle, exec, $scriptsDir/sys-touchpad.sh                              

# -----------------------
# Volume
# -----------------------
bind = , xf86audioraisevolume, exec, $scriptsDir/sys-volume.sh --inc            
bind = , xf86audiolowervolume, exec, $scriptsDir/sys-volume.sh --dec           
bind = , xf86AudioMicMute,     exec, $scriptsDir/sys-volume.sh --toggle-mic       
bind = , xf86audiomute,        exec, $scriptsDir/sys-volume.sh --toggle             

# -----------------------
# Brightness
bind = , xf86KbdBrightnessDown, exec, $scriptsDir/sys-brightness-kb.sh --dec               
bind = , xf86KbdBrightnessUp,   exec, $scriptsDir/sys-brightness-kb.sh --inc                
bind = , xf86MonBrightnessDown, exec, $scriptsDir/sys-brightness.sh --dec 
bind = , xf86MonBrightnessUp,   exec, $scriptsDir/sys-brightness.sh --inc 

# -----------------------
# Media
# -----------------------
bind = , xf86AudioPlayPause, exec, $scriptsDir/sys-media.sh --pause
bind = , xf86AudioPause,     exec, $scriptsDir/sys-media.sh --pause
bind = , xf86AudioPlay,      exec, $scriptsDir/sys-media.sh --pause
bind = , xf86AudioNext,      exec, $scriptsDir/sys-media.sh --nxt
bind = , xf86AudioPrev,      exec, $scriptsDir/sys-media.sh --prv
bind = , xf86audiostop,      exec, $scriptsDir/sys-media.sh --stop
